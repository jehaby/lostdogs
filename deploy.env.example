# Remote host credentials (required)
REMOTE_USER=ubuntu
REMOTE_HOST=example.com
# REMOTE_PATH=/home/${REMOTE_USER}/infra/lostdogs

# Image tag to deploy (optional)
# IMAGE=jehaby/lostdogs:latest

# App/runtime configuration (required)
VK_TOKEN=replace_me
TG_TOKEN=replace_me
TG_CHAT=-1001234567890
# TG_ENABLED=false
# LOG_LEVEL=info

# Optional config on remote (mount to /app/config.yml)
# REMOTE_CONFIG_PATH=/opt/lostdogs/config.yml

# DB settings (optional)
# CONTAINER_NAME=lostdogs
# DB_VOLUME_NAME=lostdogs_db_data
# DB_DIR_IN_CONTAINER=/data
# DB_CONN_STRING=file:/data/lostdogs.db?cache=shared&mode=rwc
